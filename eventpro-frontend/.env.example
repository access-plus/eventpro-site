# Copy to .env.local and fill in. Used when running frontend locally (npm run dev).
# For Docker, set STRIPE_PUBLISHABLE_KEY in the project root .env instead.
VITE_API_BASE_URL=http://localhost:8080
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
