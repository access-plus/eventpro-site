.test-service:
  image: eclipse-temurin:21-jdk-alpine
  before_script:
    - cd backend
  script:
    - echo "Testing backend"
    - ./gradlew test jacocoTestReport --info
    - echo "Generating aggregated coverage report for GitLab CI"
    - ./gradlew jacocoRootReport --info
    - echo "Test coverage report generated"
  artifacts:
    reports:
      junit: backend/services/modules/*/build/test-results/test/TEST-*.xml
    paths:
      - backend/services/modules/*/build/test-results/
      - backend/services/modules/*/build/reports/jacoco/
      - backend/services/modules/*/build/reports/tests/
      - backend/services/build/reports/jacoco/root/
    expire_in: 1 week
