.docker-build-ecr:
  image: 
    name: amazon/aws-cli:2.23.0
    entrypoint: [""]
  stage: docker
  services:
    - docker:27-dind
  variables: 
    DOCKER_HOST: tcp://docker:2375/
  before_script:
    - amazon-linux-extras install docker
    - aws --version
